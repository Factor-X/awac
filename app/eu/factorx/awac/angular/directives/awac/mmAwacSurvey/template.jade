ng-virtual
    mm-awac-survey-section(header="Chauffage")

        mm-awac-question(label-code="MAIN-FUEL-CODE", ng-reviewers="ngModel.mainFuel.reviewers")
            mm-dropdown(ng-model="ngModel.mainFuel", ng-options="ngModel.fuels")

        mm-awac-question(label-code="CONSUMPTION-CODE", ng-reviewers="ngModel.consumption.reviewers")
            mm-double(ng-model="ngModel.consumption")
            mm-unit(ng-model="ngModel.consumption", ng-options="ngModel.volumeUnits")

        mm-awac-question(label-code="ADD-A-CONSUMPTION-CODE", ng-reviewers="ngModel.otherFuels.reviewers")

    hr

    mm-awac-survey-section(header="Habitation")

        mm-awac-question(label-code="HOUSE-TYPE-CODE", ng-reviewers="ngModel.householdType.reviewers")
            mm-double(ng-model="ngModel.householdType")

        mm-awac-question(label-code="SURFACE-CODE", ng-reviewers="ngModel.surface.reviewers")
            mm-double(ng-model="ngModel.surface")

        mm-awac-question(label-code="NUMBER-OF-INHABITANTS-CODE", ng-reviewers="ngModel.inhabitants.reviewers")
            mm-double(ng-model="ngModel.inhabitants")

        mm-awac-question(label-code="NUMBER-OF-HEATED-ROOMS-CODE", ng-reviewers="ngModel.heatedRooms.reviewers")
            mm-double(ng-model="ngModel.heatedRooms")

        mm-awac-question(label-code="BEFORE-75-CODE", ng-reviewers="ngModel.builtBefore1975.reviewers")
            mm-boolean(ng-model="ngModel.builtBefore1975")

        mm-awac-question(label-code="ISOLATION-10-LAST-YEARS-CODE")

        mm-awac-question(label-code="ROOF-CODE", ng-reviewers="ngModel.last10YearsRoof.reviewers")
            mm-boolean(ng-model="ngModel.last10YearsRoof")

        mm-awac-question(label-code="EXTERNAL-WALLS-CODE", ng-reviewers="ngModel.last10YearsExternalWalls.reviewers")
            mm-boolean(ng-model="ngModel.last10YearsExternalWalls")

        mm-awac-question(label-code="INSULATED-GLAZING-CODE", ng-reviewers="ngModel.last10YearsInsulatedGlazing.reviewers")
            mm-boolean(ng-model="ngModel.last10YearsInsulatedGlazing")

    hr

    mm-awac-survey-section(header="Cuisson")

        mm-awac-question(label-code="GAZ-COOKING-CODE", ng-reviewers="ngModel.butane.reviewers")
            mm-double(ng-model="ngModel.butane")
            mm-unit(ng-model="ngModel.butane", ng-options="ngModel.volumeUnits")

    // Chauffage
    // -- Combustible principal ?
    // -- Consommation précise
    // -- Ajouter un autre combu
    // Habitation
    // -- Type
    // -- Surface
    // -- Nombre
    // -- Nombre pièces chauffées
    // -- Construction avant 1975
    // -- Isolation 10 dernières années
    // -- -- Toit
    // -- -- Murs exétérieurs
    // -- -- Double vitrage
    // Cuisson
    // -- au butane



