    // =========================================================================
    // FORMS
    // =========================================================================

<% for f in forms %>
    // == <%= f.code %>
    // <%= f.name %>
    Form form<%= f.number %> = new Form("<%= f.code %>");
    session.saveOrUpdate(form<%= f.number %>);

<% end %>